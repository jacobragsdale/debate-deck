export type UserType = {
    displayName: string
}

export type CardType = {
    id: string | number
    tag: string
    authors: string[]
    time: any
    publisher: string
    articleTitle: string
    link: string
    tags: string[]
    quote: string
}
